# Zeabur PostgreSQL Configuration Template
# Copy these values to your .env file after successful migration

# Zeabur PostgreSQL Connection Variables
POSTGRES_HOST=${CONTAINER_HOSTNAME}
POSTGRES_PORT=${DATABASE_PORT}
POSTGRES_DATABASE=${POSTGRES_DB}
POSTGRES_USER=root
POSTGRES_USERNAME=${POSTGRES_USER}
POSTGRES_PASSWORD=gmu4K8wEY2efGP5k90il1VX7I3T6JLBh

# Prisma Database URLs for Zeabur
DATABASE_URL="postgresql://${POSTGRES_USERNAME}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DATABASE}?pgbouncer=true"
DIRECT_URL="postgresql://${POSTGRES_USERNAME}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DATABASE}"

# If environment variable substitution doesn't work, use direct values:
# DATABASE_URL="postgresql://root:gmu4K8wEY2efGP5k90il1VX7I3T6JLBh@sjc1.clusters.zeabur.com:5432/zeabur?pgbouncer=true"
# DIRECT_URL="postgresql://root:gmu4K8wEY2efGP5k90il1VX7I3T6JLBh@sjc1.clusters.zeabur.com:5432/zeabur"
